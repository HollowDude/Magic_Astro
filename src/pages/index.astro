---
// Redirigir la raíz al login (o dashboard si hay sesión)
import { getSession } from '@/services/session.service';

const session = getSession(Astro.cookies);
return Astro.redirect(session ? '/dashboard' : '/login');
---
