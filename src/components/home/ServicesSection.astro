---
// src/components/home/ServicesSection.astro
import ServicesCarousel from '@/components/home/ServicesCarousel';
import { getServicesBlockData } from '@/services/nodehive/nodehive.blocks';
import { getLangFromUrl } from '@/i18n/utils';
import type { Lang } from '@/i18n/ui';

const lang = (Astro.currentLocale ?? getLangFromUrl(Astro.url)) as Lang;
const data = await getServicesBlockData(lang);

const serviciosNormalizados = data?.servicios.map((s) => ({
  title: s.titulo,
  description: s.descripcion,
  image: s.imagen,
})) ?? [];
---

<ServicesCarousel
  titulo={data?.titulo}
  subTitulo={data?.subTitulo}
  eslogan={null}
  services={serviciosNormalizados}
  client:load
/>