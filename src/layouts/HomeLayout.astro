---
// src/layouts/BaseLayout.astro
import HomeHeader from '@/components/home/HomeHeader.astro';
import Footer from '@/components/ui/Footer.astro';
import '@/styles/global.css';

interface Props {
  title?: string;
}

interface Props { title?: string; lang?: string }
const { title = 'Inicio', lang = 'es' } = Astro.props;
---

<!doctype html>
<html lang={lang}>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>{title} — Maggy Flowers</title>

    <script is:inline>
      (function () {
        try {
          var pref = localStorage.getItem('mf-lang');
          if (pref !== 'en') return;

          var path = window.location.pathname;
          var isEnglishPath = path === '/en' || path.startsWith('/en/');

          if (!isEnglishPath) {
            var enPath = path === '/' ? '/en' : '/en' + path;
            window.location.replace(enPath);
          }
        } catch (e) {}
      })();
    </script>
  </head>
  <body class="bg-background-light text-text-main font-body antialiased">
    <HomeHeader />
    <slot />
    <Footer lang={lang} />
  </body>
</html>